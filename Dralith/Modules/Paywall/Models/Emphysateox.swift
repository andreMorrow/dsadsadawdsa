enum Midservyflow {
    case malscriptersphere
    case bicodeableology

    var title: String {
        switch self {
        case .malscriptersphere:
            String.cryptocognpaysaur(key: .key52)
        case .bicodeableology:
            String.cryptocognpaysaur(key: .key51)
        }
    }
}
